# Compatibility

This skill targets the portable Agent Skills format.

Compatibility checks cover:
- Required YAML frontmatter with name and description
- Lowercase hyphenated skill identity matching the directory
- Required SKILL.md workflow sections
- Referenced templates and documents existing
- No unfinished scaffold placeholders

The workflow is agent-neutral. Platform-specific guidance should be added by a consuming agent or a separate reference file.
